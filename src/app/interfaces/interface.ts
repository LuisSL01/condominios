export interface Componente {
    icon: string;
    name: string;
    redirectTo: string;
  }

/* export interface Aviso{
  destinatario: string;
  titulo: string;
  fechaVencimiento: Date;
  mensaje: string;
} */

